---
eslint:
  stage: lint
  image:
    name: megabytelabs/eslint:slim
    entrypoint: ['']
  allow_failure: true
  variables:
    GIT_DEPTH: 1
  script:
    - set -eo pipefail
    - eslint --format gitlab . # Requires eslint-formatter-gitlab
